
<div class="page-header">
  <h1>
  All My Available Survey Questions
    <a href="/questions/new" class="btn btn-lg btn-success">New Survey</a>
  </h1>


<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Body</th>
        <th>Option 1</th>
        <th>Option 2</th>
        <th>Name</th>
        <th>Restaurant</th>
        <th>Actions</th>
      </tr>



      <% @questions.each do |question| %>

      <%if  question.restaurant.user.id == current_user.id %>

      <tr>
        <td><%= question.body %></td>
        <td><%= question.option_1 %></td>
        <td><%= question.option_2 %></td>
        <td><%= question.name %></td>
        <td>
          <% if question.restaurant.present? %>
            <a href="/restaurants/<%= question.restaurant.id %>">
              <%= question.restaurant.name %>
            </a>
          <% end %>
        </td>
        <td>
          <a href="/questions/<%= question.id %>" class="btn btn-primary">Browse Results</a>
          <a href="/questions/<%= question.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_question/<%= question.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>

        <%else%>
      <% end %>

      </tr>
      <% end %>
    </table>
    <%= paginate @questions, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
